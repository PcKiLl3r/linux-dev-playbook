---
dependency:
  name: galaxy
driver:
  name: default
lint: |
  set -e
  yamllint .
  ansible-lint
platforms:
  - name: instance
    image: geerlingguy/docker-fedora40-ansible
    #    command: ""
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    priviliged: true
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
